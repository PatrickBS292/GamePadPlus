<Page x:Class="GamePadPlus.GameWorkspacePage"
      Loaded="Page_Loaded"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:GamePadPlus"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="GameWorkspacePage"
      Background="#1E2329">

    <Page.Resources>

        <Style x:Key="WorkspaceButtonStyle"
               TargetType="Button">

            <Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="Background" Value="#303740"/>
            <Setter Property="BorderBrush" Value="#3A414A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter
                                    TargetName="ButtonBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">

                                <Setter
                                    TargetName="ButtonBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                                <Setter
                                    TargetName="ButtonBorder"
                                    Property="BorderThickness"
                                    Value="2"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>


        <Style x:Key="FormattingToggleStyle"
               TargetType="ToggleButton">

            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="Background" Value="#303740"/>
            <Setter Property="BorderBrush" Value="#3A414A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="ToggleButton">

                        <Border
                            x:Name="ToggleBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="7">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextElement.Foreground="{TemplateBinding Foreground}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter
                                    TargetName="ToggleBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                            </Trigger>

                            <Trigger Property="IsChecked"
                                     Value="True">

                                <Setter
                                    TargetName="ToggleBorder"
                                    Property="Background"
                                    Value="#FF8A3D"/>

                                <Setter
                                    TargetName="ToggleBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                                <Setter
                                    Property="Foreground"
                                    Value="#1E2329"/>

                            </Trigger>

                            <MultiDataTrigger>

                                <MultiDataTrigger.Conditions>

                                    <Condition
                                        Binding="{Binding IsChecked,
                                            RelativeSource={RelativeSource Self}}"
                                        Value="True"/>

                                    <Condition
                                        Binding="{Binding IsMouseOver,
                                            RelativeSource={RelativeSource Self}}"
                                        Value="True"/>

                                </MultiDataTrigger.Conditions>

                                <Setter
                                    TargetName="ToggleBorder"
                                    Property="BorderBrush"
                                    Value="#FFB06A"/>

                            </MultiDataTrigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>


        <Style x:Key="FormattingButtonStyle"
               TargetType="Button">

            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="Background" Value="#303740"/>
            <Setter Property="BorderBrush" Value="#3A414A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border
                            x:Name="FormattingBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="7">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextElement.Foreground="{TemplateBinding Foreground}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter
                                    TargetName="FormattingBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">

                                <Setter
                                    TargetName="FormattingBorder"
                                    Property="BorderBrush"
                                    Value="#FF8A3D"/>

                                <Setter
                                    TargetName="FormattingBorder"
                                    Property="BorderThickness"
                                    Value="2"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>


        <Style x:Key="FontSizeComboStyle"
       TargetType="ComboBox">

            <Setter Property="Width" Value="78"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="Background" Value="#303740"/>
            <Setter Property="BorderBrush" Value="#3A414A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="ComboBox">

                        <Grid>

                            <ToggleButton
                        x:Name="DropDownButton"
                        Focusable="False"
                        ClickMode="Press"
                        IsChecked="{Binding IsDropDownOpen,
                            RelativeSource={RelativeSource TemplatedParent},
                            Mode=TwoWay}">

                                <ToggleButton.Template>

                                    <ControlTemplate TargetType="ToggleButton">

                                        <Border
                                    x:Name="ComboBorder"
                                    Background="#303740"
                                    BorderBrush="#3A414A"
                                    BorderThickness="1"
                                    CornerRadius="7">

                                            <Grid>

                                                <ContentPresenter
                                            Margin="8,0,25,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Content="{Binding SelectionBoxItem,
                                                RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            TextElement.Foreground="#E8E8E8"/>

                                                <TextBlock
                                            Text="▼"
                                            FontSize="9"
                                            Foreground="#E8E8E8"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            Margin="0,0,9,0"/>

                                            </Grid>

                                        </Border>

                                        <ControlTemplate.Triggers>

                                            <Trigger Property="IsMouseOver"
                                             Value="True">

                                                <Setter
                                            TargetName="ComboBorder"
                                            Property="BorderBrush"
                                            Value="#FF8A3D"/>

                                            </Trigger>

                                            <Trigger Property="IsChecked"
                                             Value="True">

                                                <Setter
                                            TargetName="ComboBorder"
                                            Property="BorderBrush"
                                            Value="#FF8A3D"/>

                                            </Trigger>

                                        </ControlTemplate.Triggers>

                                    </ControlTemplate>

                                </ToggleButton.Template>

                            </ToggleButton>


                            <Popup
                        x:Name="PART_Popup"
                        Placement="Bottom"
                        AllowsTransparency="True"
                        Focusable="False"
                        IsOpen="{TemplateBinding IsDropDownOpen}"
                        StaysOpen="False">

                                <Border
                            Background="#252B32"
                            BorderBrush="#3A414A"
                            BorderThickness="1"
                            CornerRadius="7"
                            MinWidth="78"
                            Margin="0,5,0,0">

                                    <ScrollViewer
                                MaxHeight="250"
                                VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Disabled">

                                        <ItemsPresenter/>

                                    </ScrollViewer>

                                </Border>

                            </Popup>

                        </Grid>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>


        <Style TargetType="ComboBoxItem">

            <Setter Property="Foreground"
                    Value="#E8E8E8"/>

            <Setter Property="Background"
                    Value="#252B32"/>

            <Setter Property="Padding"
                    Value="10,7"/>

            <Setter Property="HorizontalContentAlignment"
                    Value="Left"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="ComboBoxItem">

                        <Border
                            x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5"
                            Margin="3">

                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger
                                Property="IsMouseOver"
                                Value="True">

                                <Setter
                                    TargetName="ItemBorder"
                                    Property="Background"
                                    Value="#303740"/>

                            </Trigger>

                            <Trigger
                                Property="IsSelected"
                                Value="True">

                                <Setter
                                    TargetName="ItemBorder"
                                    Property="Background"
                                    Value="#303740"/>

                                <Setter
                                    Property="Foreground"
                                    Value="#FF8A3D"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

    </Page.Resources>


    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Button
                Content="← Back"
                Width="95"
                Style="{StaticResource WorkspaceButtonStyle}"
                Click="Back_Click"
                Foreground="#FF8A3D"/>


            <TextBlock
                x:Name="GameTitle"
                Grid.Column="1"
                FontSize="30"
                FontWeight="Bold"
                Foreground="#FF8A3D"
                VerticalAlignment="Center"
                Margin="20,0,0,0"/>

        </Grid>


        <Grid
            Grid.Row="1"
            Margin="0,25,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Border
                Width="250"
                Height="140"
                Background="#252B32"
                BorderBrush="#3A414A"
                BorderThickness="1"
                CornerRadius="10">

                <Image
                    x:Name="CoverImage"
                    Stretch="UniformToFill"/>

            </Border>


            <Button
                Grid.Column="1"
                Content="Choose Cover"
                Width="125"
                Height="38"
                Margin="15,0,0,0"
                VerticalAlignment="Bottom"
                Style="{StaticResource WorkspaceButtonStyle}"
                Click="ChooseCover_Click"
                Foreground="#FF8A3D"/>

        </Grid>


        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            Margin="0,25,0,0">


            <ToggleButton
                x:Name="BoldButton"
                Content="B"
                FontWeight="Bold"
                Style="{StaticResource FormattingToggleStyle}"
                Focusable="False"
                Click="Bold_Click"
                Foreground="#FF8A3D"/>


            <ToggleButton
                x:Name="ItalicButton"
                Content="I"
                FontStyle="Italic"
                Margin="6,0,0,0"
                Style="{StaticResource FormattingToggleStyle}"
                Focusable="False"
                Click="Italic_Click"
                Foreground="#FF8A3D"/>


            <ToggleButton
                x:Name="UnderlineButton"
                Content="U"
                Margin="6,0,0,0"
                Style="{StaticResource FormattingToggleStyle}"
                Focusable="False"
                Click="Underline_Click"
                Foreground="#FF8A3D"/>


            <ComboBox
                x:Name="FontSizeBox"
                Margin="15,0,0,0"
                Style="{StaticResource FontSizeComboStyle}"
                SelectionChanged="FontSizeBox_SelectionChanged">

                <ComboBoxItem Content="12"/>
                <ComboBoxItem Content="14"/>
                <ComboBoxItem Content="16"/>
                <ComboBoxItem Content="18"/>
                <ComboBoxItem Content="20"/>
                <ComboBoxItem Content="24"/>
                <ComboBoxItem Content="28"/>
                <ComboBoxItem Content="32"/>

            </ComboBox>


            <Button
                x:Name="BulletButton"
                Content="•"
                FontSize="18"
                Margin="15,0,0,0"
                Style="{StaticResource FormattingButtonStyle}"
                Click="Bullet_Click"
                Foreground="#FF8A3D"/>

        </StackPanel>


        <Border
            Grid.Row="3"
            Margin="0,20,0,0"
            Background="#252B32"
            BorderBrush="#3A414A"
            BorderThickness="1"
            CornerRadius="10">

            <RichTextBox
                x:Name="NotesBox"
                Background="Transparent"
                Foreground="#E8E8E8"
                BorderThickness="0"
                Padding="15"
                VerticalScrollBarVisibility="Auto"
                FontSize="16"
                TextChanged="NotesBox_TextChanged"
                SelectionChanged="NotesBox_SelectionChanged"
                MouseLeftButtonUp="NotesBox_MouseLeftButtonUp"/>

        </Border>

    </Grid>

</Page>